import 'package:flutter/material.dart';

class SustitucionScreen extends StatelessWidget {
  static const name = 'sustitucion_screen';

  const SustitucionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sustitucion Screen'),
      ),
      body: Placeholder(),
    );
  }
}
